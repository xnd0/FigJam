<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>FigJam!</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <p>{{ format_date }}</p>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-around align-center p-2">
      <img class = "fig" aria-label="figgin' cool" src = "/images/st,small,507x507-pad,600x600,f8f8f8.jpg"/>
      <h1>
        <a href="/">FigJam</a>
      </h1>
      <nav>
        {{!-- nav links to other App pages --}}
        <a href="/" class="outline" >homepage</a>
        <a href="/songlist" class="outline">songlist</a>
        <a href="/add-song" class="outline">add-song</a>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile" class="outline">profile</a>
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login" class="outline">login</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      <p id="footer">@FigJam 🎼 Project 2022</p>
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
